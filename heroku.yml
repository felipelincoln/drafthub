setup:
    addons:
        - plan: heroku-postgresql
build:
    docker:
        web: Dockerfile
release:
    image: web
    command:
        - mkdir code/static
        - python manage.py collectstatic --noinput
run:
    web: gunicorn drafthub.wsgi
